# Troubleshooting guides

Start with the observed symptom, use the checks to locate the failing layer,
then verify recovery from the affected client.

| Symptom | Guide |
| --- | --- |
| Local network works, but Remote connection is Unreachable | [Managed remote connection](remote-connection.md) |
